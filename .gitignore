.env
.env.*

# python
__pycache__/
*.py[cod]

# local data (do not commit)
ExpertServices.json

# (avoid ignoring all .json files; we keep source/config json in repo)
# virtualenv
.venv/

# local models / caches
models/.cache/
models/piper/.cache/

